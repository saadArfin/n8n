# procs:
#   inngest:
#     cmd: ["npm", "run", "inngest:dev"]

#   "next":
#     cmd: ["npm", "run", "dev"]


procs:
  inngest:
    cmd: ["cmd", "/c", "npm run inngest:dev"]

  next:
    cmd: ["cmd", "/c", "npm run dev"]


    
